<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Crashlytics9"
             x:Class="Crashlytics9.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />

		<Button Text="Crash test"
				Command="{Binding Path=CrashTest}"/>
		
		<Button Text="Force UI Crash"
				Command="{Binding Path=ForceUICrash}"/>
		
		<Button Text="Force Background Crash"
				Command="{Binding Path=ForceBackgroundCrash}"/>
	</StackLayout>

</ContentPage>
